// ===========================================================================
// SUKUNA 【MEGUMI'S BODY】 — VARIANT MODEL REFERENCE SHEET
// ===========================================================================
// Researched 2026-08-04 (web search, no images). AUDITED 2026-08-05 under the
// Part 2 rule and PASSED without changes.
//
// Sukuna's main body from chapter 212 onward, taken with the "Enchain" binding
// vow. ("Meguna" is a fan name with no canonical equivalent — see the variant
// descriptor.) Same construction as the Yuji vessel and passing for the same
// reason: the HOST'S mesh is correctly reused because in the fiction it IS the
// host's body, and the variant's own geometry is the overlay — markings, the
// second pair of eyes below the natural pair, and the arm and wrist bands, all
// parented to Megumi's bones and scaled to his head.
//
// The two vessels differ from each other exactly as Yuji and Megumi differ from
// each other, which is the correct amount: they are two different people being
// worn by the same curse.
// ===========================================================================
export { buildSukunaMegumi } from '../sukuna_vessel.js';
